function mostrar()
{
	let edad;

	edad = parseInt(document.getElementById("txtIdEdad").value);

	if(edad<18 && (document.getElementById("estadoCivil").))
	{
		alert("Es muy pequeño para NO ser soltero.")
	}
}